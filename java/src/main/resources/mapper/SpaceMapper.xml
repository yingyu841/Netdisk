<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.netdisk.mapper.SpaceMapper">

    <select id="findPersonalByOwnerUserId" resultType="com.netdisk.pojo.entity.Space">
        SELECT id, space_uuid, owner_user_id, name, space_type, status, created_at, updated_at
        FROM spaces
        WHERE owner_user_id = #{ownerUserId}
          AND space_type = 'personal'
          AND status = 1
        ORDER BY id ASC
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO spaces (space_uuid, owner_user_id, name, space_type, status, created_at, updated_at)
        VALUES (#{spaceUuid}, #{ownerUserId}, #{name}, #{spaceType}, #{status}, NOW(), NOW())
    </insert>

    <select id="findPersonalSpaceUsage" resultType="java.util.HashMap">
        SELECT
            COALESCE(s.quota_bytes, 0) AS quotaBytes,
            COALESCE(SUM(r.size_bytes), 0) AS usedBytes
        FROM spaces s
        LEFT JOIN resources r ON r.space_id = s.id AND r.is_deleted = 0
        WHERE s.owner_user_id = #{ownerUserId}
          AND s.space_type = 'personal'
          AND s.status = 1
        GROUP BY s.id, s.quota_bytes
    </select>

</mapper>
